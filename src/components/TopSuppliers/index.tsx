import React from 'react';
import styles from './TopSuppliers.module.scss';

const TopSuppliers: React.FC = () => {
    return (
        <div className={styles.wrapper}>
            Hello from TopSuppliers
        </div>
    );
}

export default TopSuppliers;
